@inject Microsoft.AspNetCore.Antiforgery.IAntiforgery Antiforgery
@inject Platform.Infrastructure.Definitions.AppDefinitions Defs
@{
    var currentLang = Context.Request.Query["lang"].FirstOrDefault() ?? "en";
    var tokens = Antiforgery.GetAndStoreTokens(Context);
    var models = Defs.Models;
    var pages = Defs.Pages;
    var projectName = Context.Request.Query["project"].FirstOrDefault() ?? "app";
}

<!DOCTYPE html>
<html lang="@currentLang">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>@ViewData["Title"] - Low Code Platform</title>
    <link rel="stylesheet" href="~/css/site.css" />
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" />
    <script src="https://cdn.tailwindcss.com"></script>
    <script src="https://unpkg.com/htmx.org@1.9.10"></script>
</head>
<body>
    <input type="hidden" id="csrf-token" value="@tokens.RequestToken" />
    <script src="~/js/site.js"></script>
    <nav class="navbar">
        <div class="navbar-top">
            <button id="mobile-menu-toggle" type="button" class="mobile-menu-toggle" aria-label="Toggle menu" aria-expanded="false" onclick="toggleMobileMenu()">
                <i class="fas fa-bars"></i>
            </button>
            <a href="/" class="navbar-brand mobile-close-on-click">
                <i class="fas fa-cube"></i>
                Low Code Platform
            </a>
        </div>

        <div class="desktop-nav">
            <!-- 项目切换菜单 -->
            <div class="desktop-menu">
                <button type="button" class="desktop-menu-trigger" onclick="toggleDesktopMenu('desktop-project-menu', this)">
                    <i class="fas fa-folder"></i>
                    @(currentLang == "zh" ? "项目" : "Projects")
                    <i class="fas fa-chevron-down text-xs"></i>
                </button>
                <div id="desktop-project-menu" class="desktop-menu-panel">
                    <div class="desktop-menu-panel-inner">
                        <div class="desktop-menu-section">
                            <h4>@(currentLang == "zh" ? "选择项目" : "Select Project")</h4>
                            <div class="desktop-menu-links">
                                <a href="/?project=todo" class="desktop-menu-link">
                                    📋 TODO @(currentLang == "zh" ? "项目管理" : "Project")
                                </a>
                                <a href="/?project=journal" class="desktop-menu-link">
                                    📔 @(currentLang == "zh" ? "我的日记本" : "Journal")
                                </a>
                                <a href="/?project=chinook" class="desktop-menu-link">
                                    🎵 Chinook @(currentLang == "zh" ? "音乐商店" : "Music")
                                </a>
                                <a href="/?project=ecommerce" class="desktop-menu-link">
                                    🛒 @(currentLang == "zh" ? "电商订单" : "E-commerce")
                                </a>
                                <a href="/?project=crm" class="desktop-menu-link">
                                    💼 @(currentLang == "zh" ? "CRM 客户管理" : "CRM")
                                </a>
                            </div>
                        </div>
                    </div>
                </div>
            </div>

            <div class="desktop-menu">
                <button type="button" class="desktop-menu-trigger" onclick="toggleDesktopMenu('desktop-user-menu', this)">
                    <i class="fas fa-user-circle"></i>
                    用户状态
                    <i class="fas fa-chevron-down text-xs"></i>
                </button>
                <div id="desktop-user-menu" class="desktop-menu-panel">
                    <div class="desktop-menu-panel-inner">
                        <div class="user-status-display">
                            <div class="guest-user">
                                <p>加载中...</p>
                            </div>
                        </div>
                    </div>
                </div>
            </div>

            <div class="desktop-menu">
                <button type="button" class="desktop-menu-trigger" onclick="toggleDesktopMenu('desktop-lang-menu', this)">
                    <i class="fas fa-language"></i>
                    多语言
                    <i class="fas fa-chevron-down text-xs"></i>
                </button>
                <div id="desktop-lang-menu" class="desktop-menu-panel">
                    <div class="desktop-menu-panel-inner">
                        <div class="desktop-menu-section">
                            <h4>选择语言</h4>
                            <div class="desktop-menu-links">
                                <button type="button" class="desktop-menu-link-btn" onclick="changeLanguage('en')">🇺🇸 English</button>
                                <button type="button" class="desktop-menu-link-btn" onclick="changeLanguage('zh')">🇨🇳 中文</button>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>

        <ul id="main-nav" class="navbar-nav mobile-nav">
            <li class="mobile-menu-header">
                <span class="mobile-menu-title">Menu</span>
                <button id="mobile-menu-close" type="button" class="mobile-menu-close" aria-label="Close menu" onclick="closeMobileMenu()">
                    <i class="fas fa-times"></i>
                </button>
            </li>
            <li class="nav-item">
                <a href="/" class="nav-link mobile-close-on-click @(ViewData["ActivePage"]?.ToString() == "Home" ? "active" : "")">
                    <i class="fas fa-home"></i>
                    @(currentLang == "zh" ? "首页" : "Home")
                </a>
            </li>

            <!-- 项目切换菜单 -->
            <li class="nav-item dropdown">
                <a href="#" class="nav-link dropdown-toggle">
                    <i class="fas fa-folder"></i>
                    @(currentLang == "zh" ? "项目" : "Projects")
                    <i class="fas fa-chevron-down ml-1 text-xs"></i>
                </a>
                <div class="dropdown-menu">
                    <a href="/?project=todo" class="dropdown-item mobile-close-on-click">
                        <i class="fas fa-folder mr-2"></i>
                        📋 TODO @(currentLang == "zh" ? "项目管理" : "Project")
                    </a>
                    <a href="/?project=journal" class="dropdown-item mobile-close-on-click">
                        <i class="fas fa-folder mr-2"></i>
                        📔 @(currentLang == "zh" ? "我的日记本" : "Journal")
                    </a>
                    <a href="/?project=chinook" class="dropdown-item mobile-close-on-click">
                        <i class="fas fa-folder mr-2"></i>
                        🎵 Chinook @(currentLang == "zh" ? "音乐商店" : "Music Store")
                    </a>
                    <a href="/?project=ecommerce" class="dropdown-item mobile-close-on-click">
                        <i class="fas fa-folder mr-2"></i>
                        🛒 @(currentLang == "zh" ? "电商订单系统" : "E-commerce")
                    </a>
                    <a href="/?project=crm" class="dropdown-item mobile-close-on-click">
                        <i class="fas fa-folder mr-2"></i>
                        💼 @(currentLang == "zh" ? "CRM 客户管理" : "CRM")
                    </a>
                </div>
            </li>

            <!-- 数据模型菜单 -->
            @if (models != null && models.Any())
            {
                <li class="nav-item dropdown">
                    <a href="#" class="nav-link dropdown-toggle">
                        <i class="fas fa-database"></i>
                        @(currentLang == "zh" ? "数据管理" : "Data Management")
                        <i class="fas fa-chevron-down ml-1 text-xs"></i>
                    </a>
                    <div class="dropdown-menu">
                        @foreach (var model in models)
                        {
                            var uiLabels = model.Value.Ui?.Labels;
                            string displayName = model.Key;
                            
                            if (uiLabels != null)
                            {
                                var labels = currentLang == "zh" && uiLabels.Zh != null ? uiLabels.Zh : uiLabels.En;
                                if (labels != null && labels.ContainsKey("title"))
                                {
                                    displayName = labels["title"];
                                }
                            }
                            
                            <a href="/ui/@(model.Key)?project=@projectName&lang=@currentLang" class="dropdown-item mobile-close-on-click">
                                <i class="fas fa-table mr-2"></i>
                                @displayName
                            </a>
                        }
                    </div>
                </li>
            }

            <!-- 多表页面菜单 -->
            @if (pages != null && pages.Any())
            {
                <li class="nav-item dropdown">
                    <a href="#" class="nav-link dropdown-toggle">
                        <i class="fas fa-th-large"></i>
                        @(currentLang == "zh" ? "多表页面" : "Multi-Table Pages")
                        <i class="fas fa-chevron-down ml-1 text-xs"></i>
                    </a>
                    <div class="dropdown-menu">
                        @foreach (var page in pages)
                        {
                            var pageTitle = page.Value.Title ?? page.Key;
                            <a href="/page/@(page.Key)?project=@projectName&lang=@currentLang" class="dropdown-item mobile-close-on-click">
                                <i class="fas fa-file-alt mr-2"></i>
                                @pageTitle
                            </a>
                        }
                    </div>
                </li>
            }

            <!-- AI 功能菜单 -->
            <li class="nav-item dropdown">
                <a href="#" class="nav-link dropdown-toggle">
                    <i class="fas fa-robot"></i>
                    @(currentLang == "zh" ? "AI 功能" : "AI Features")
                    <i class="fas fa-chevron-down ml-1 text-xs"></i>
                </a>
                <div class="dropdown-menu">
                    <a href="/InventoryAiPage" class="dropdown-item mobile-close-on-click">
                        <i class="fas fa-chart-line mr-2"></i>
                        @(currentLang == "zh" ? "库存 AI 分析" : "Inventory AI")
                    </a>
                    <a href="/ai-validation" class="dropdown-item mobile-close-on-click">
                        <i class="fas fa-shield-alt mr-2"></i>
                        @(currentLang == "zh" ? "AI 验证" : "AI Validation")
                    </a>
                    <a href="/ai-validation/snapshots" class="dropdown-item mobile-close-on-click">
                        <i class="fas fa-cogs mr-2"></i>
                        @(currentLang == "zh" ? "快照管理" : "Snapshots")
                    </a>
                    <a href="/ai-validation/test-scenarios" class="dropdown-item mobile-close-on-click">
                        <i class="fas fa-flask mr-2"></i>
                        @(currentLang == "zh" ? "测试场景" : "Test Scenarios")
                    </a>
                </div>
            </li>

            <li class="nav-item">
                <a href="/docs" class="nav-link mobile-close-on-click @(ViewData["ActivePage"]?.ToString() == "Docs" ? "active" : "")">
                    <i class="fas fa-book"></i>
                    @(currentLang == "zh" ? "文档" : "Docs")
                </a>
            </li>

            <!-- 用户认证菜单 -->
            <li class="nav-item" id="mobile-auth-menu">
                <!-- 由 JavaScript 动态填充 -->
            </li>

            <li class="nav-item">
                <div class="language-switcher">
                    <select onchange="changeLanguage(this.value)" class="bg-transparent border-none focus:ring-0">
                        <option value="en" @(currentLang == "en" ? "selected" : "")>🇺🇸 EN</option>
                        <option value="zh" @(currentLang == "zh" ? "selected" : "")>🇨🇳 中文</option>
                    </select>
                </div>
            </li>
        </ul>
    </nav>
    <div id="mobile-menu-backdrop" class="mobile-menu-backdrop" onclick="closeMobileMenu()"></div>

    <main class="main-content">
        @RenderBody()
        @await RenderSectionAsync("Scripts", required: false)
    </main>

    @await Html.PartialAsync("_DeleteDialog")
    @await Html.PartialAsync("_ErrorDialog")

    <footer class="bg-gray-800 text-white py-8">
        <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
            <div class="text-center">
                <p class="text-lg font-medium">Low Code Platform</p>
                <p class="mt-2 text-gray-400">Powered by Modern Web Technologies</p>
                <p class="mt-4 text-sm text-gray-500">&copy; 2026 Low Code Platform. All rights reserved.</p>
            </div>
        </div>
    </footer>

    <script src="~/js/site.js"></script>
    <script>
        function closeMobileMenu() {
            document.body.classList.remove('mobile-menu-open');
            const toggle = document.getElementById('mobile-menu-toggle');
            if (toggle) {
                toggle.setAttribute('aria-expanded', 'false');
                toggle.innerHTML = '<i class="fas fa-bars"></i>';
            }
            document.querySelectorAll('.dropdown.active').forEach(el => el.classList.remove('active'));
        }

        function closeDesktopMenus() {
            document.querySelectorAll('.desktop-menu-panel.open').forEach(el => el.classList.remove('open'));
            document.querySelectorAll('.desktop-menu-trigger.active').forEach(el => el.classList.remove('active'));
        }

        function toggleDesktopMenu(menuId, trigger) {
            if (window.innerWidth <= 768) return;

            const menu = document.getElementById(menuId);
            if (!menu) return;
            const isOpen = menu.classList.contains('open');

            closeDesktopMenus();
            if (!isOpen) {
                menu.classList.add('open');
                if (trigger) trigger.classList.add('active');
            }
        }

        function toggleMobileMenu() {
            closeDesktopMenus();
            const isOpen = document.body.classList.toggle('mobile-menu-open');
            const toggle = document.getElementById('mobile-menu-toggle');
            if (toggle) {
                toggle.setAttribute('aria-expanded', isOpen ? 'true' : 'false');
                toggle.innerHTML = isOpen ? '<i class="fas fa-times"></i>' : '<i class="fas fa-bars"></i>';
            }
            if (!isOpen) {
                document.querySelectorAll('.dropdown.active').forEach(el => el.classList.remove('active'));
            }
        }

        document.addEventListener('DOMContentLoaded', function() {
            document.querySelectorAll('.dropdown-toggle').forEach(function(el) {
                el.addEventListener('click', function(event) {
                    if (window.innerWidth > 768) return;
                    event.preventDefault();
                    const parent = el.closest('.dropdown');
                    if (!parent) return;
                    parent.classList.toggle('active');
                });
            });

            document.querySelectorAll('.mobile-close-on-click').forEach(function(el) {
                el.addEventListener('click', function() {
                    if (window.innerWidth <= 768) closeMobileMenu();
                });
            });

            document.addEventListener('keydown', function(event) {
                if (event.key === 'Escape') {
                    closeMobileMenu();
                    closeDesktopMenus();
                }
            });

            document.addEventListener('click', function(event) {
                if (!event.target.closest('.desktop-menu')) {
                    closeDesktopMenus();
                }
            });
        });

        window.addEventListener('resize', function() {
            if (window.innerWidth > 768) {
                closeMobileMenu();
            }
            if (window.innerWidth <= 768) {
                closeDesktopMenus();
            }
        });

        function changeLanguage(lang) {
            const url = new URL(window.location);
            url.searchParams.set('lang', lang);
            window.location = url.toString();
        }
    </script>

    <!-- 用户状态组件 -->
    @await Html.PartialAsync("_UserStatus")
</body>
</html>
