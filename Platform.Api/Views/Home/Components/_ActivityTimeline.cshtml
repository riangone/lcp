@model dynamic
@{
    var currentLang = Context.Request.Query["lang"].FirstOrDefault() ?? "en";
    var title = Model?.Title?.GetText(currentLang) ?? "";
    var subtitle = Model?.Subtitle?.GetText(currentLang) ?? "";
    var limit = Model?.Limit ?? 10;
    
    // 模拟活动数据（实际应从数据库获取）
    var activities = new List<dynamic>
    {
        new { Icon = "📞", Title = "电话跟进", Customer = "科技有限公司", Time = "10 分钟前", Type = "call" },
        new { Icon = "📧", Title = "发送邮件", Customer = "贸易集团", Time = "1 小时前", Type = "email" },
        new { Icon = "🤝", Title = "客户会面", Customer = "制造股份", Time = "2 小时前", Type = "meeting" },
        new { Icon = "📄", Title = "创建报价", Customer = "零售公司", Time = "3 小时前", Type = "quote" },
        new { Icon = "✅", Title = "商机关闭", Customer = "服务企业", Time = "5 小时前", Type = "won" },
    };
}

<div class="bg-white py-8">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        @if (!string.IsNullOrEmpty(title))
        {
            <div class="text-center mb-8">
                <h3 class="text-2xl font-bold text-gray-800">@title</h3>
                @if (!string.IsNullOrEmpty(subtitle))
                {
                    <p class="text-gray-600 mt-2">@subtitle</p>
                }
            </div>
        }
        
        <div class="max-w-3xl mx-auto">
            <div class="flow-root">
                <ul class="-mb-8">
                    @for (int i = 0; i < activities.Count; i++)
                    {
                        var activity = activities[i];
                        var isLast = i == activities.Count - 1;
                        
                        <li>
                            <div class="relative pb-8">
                                @if (!isLast)
                                {
                                    <span class="absolute top-4 left-4 -ml-px h-full w-0.5 bg-gray-200" aria-hidden="true"></span>
                                }
                                <div class="relative flex space-x-3">
                                    <div class="w-8 h-8 bg-gradient-to-br from-blue-400 to-blue-600 rounded-full flex items-center justify-center text-lg shadow-lg flex-shrink-0">
                                        @activity.Icon
                                    </div>
                                    <div class="flex min-w-0 flex-1 justify-between space-x-4">
                                        <div class="flex-1">
                                            <p class="text-sm font-medium text-gray-900">
                                                @activity.Title - <span class="text-blue-600">@activity.Customer</span>
                                            </p>
                                        </div>
                                        <div class="text-sm text-gray-500 whitespace-nowrap">
                                            @activity.Time
                                        </div>
                                    </div>
                                </div>
                            </div>
                        </li>
                    }
                </ul>
            </div>
            
            <!-- 查看更多 -->
            <div class="text-center mt-8">
                <a href="/ui/Activity?project=crm" class="text-blue-600 hover:text-blue-700 font-medium">
                    查看全部活动 <span aria-hidden="true">→</span>
                </a>
            </div>
        </div>
    </div>
</div>
