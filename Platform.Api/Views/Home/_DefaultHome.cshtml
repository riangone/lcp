<div class="bg-gradient-to-br from-blue-50 via-white to-purple-50">
    <div class="container mx-auto px-4 pt-16 pb-8 text-center">
        <h1 class="text-5xl font-bold text-gray-800 mb-4">🚀 LowCode Platform</h1>
        <p class="text-xl text-gray-600 max-w-2xl mx-auto">YAML-driven low-code platform</p>
    </div>
    <div class="container mx-auto px-4 pb-12">
        <h2 class="text-2xl font-bold text-gray-800 mb-6 text-center">Data Models</h2>
        <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-6">
            @foreach (var model in (ViewData["Models"] as Dictionary<string, Platform.Infrastructure.Definitions.ModelDefinition>) ?? new Dictionary<string, Platform.Infrastructure.Definitions.ModelDefinition>())
            {
                <a href="/@(model.Key)?project=@ViewData["ProjectName"]" class="bg-white rounded-xl p-6 shadow-md hover:shadow-lg transition">
                    <div class="text-center"><div class="text-3xl mb-2">📊</div><h3 class="text-xl font-semibold">@(model.Key)</h3></div>
                </a>
            }
        </div>
    </div>
</div>
